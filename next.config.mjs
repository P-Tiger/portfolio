/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  // output: 'standalone', // Bật lại nếu deploy Docker/Oracle. Vercel không cần.
};

export default nextConfig;
